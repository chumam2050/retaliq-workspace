# --- Basic Settings ---
listen_addresses = '*' # Listen on all network interfaces
max_connections = 100

# --- Authentication ---
# Must match pg_hba.conf (scram-sha-256 is the default in PG14+)
password_encryption = scram-sha-256
